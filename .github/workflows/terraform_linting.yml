name: 'base'
permissions:
  contents: read

on:
  push:
    branches: [ main ]

jobs:
    terraform-job:
        uses: fivexl/github-reusable-workflows/.github/workflows/terraform-job.yml@replace-tfsec-with-trivy
        with:
            terraform-version: "1.7.0"
            aws-default-region: "eu-central-1"
